customtkinter>=5.2.0
pynput>=1.7.6
pywin32>=306
pillow>=10.0.0
numpy>=1.24.0
easyocr>=1.7.0           # Primary OCR engine (AI neural-network)
pytesseract>=0.3.10      # Fallback OCR (requires Tesseract-OCR installed separately)
opencv-python>=4.8.0     # Image template matching for conditional plan steps
